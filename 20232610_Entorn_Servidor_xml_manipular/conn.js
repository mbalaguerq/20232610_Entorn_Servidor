//posem tres ordres: 

//1-Conexió. Capturem el fitxer amb get o post
//dins el propi GET definim la funció anònima (data, status, etc) 
//Data emmagatzema el fitxer sencer.

/*$.get("nota.xml", function(data){

    //posem data dins una variable.
    //amb el find i ("etiqueta") ens busca les etiquetes. 
    //.text ens dona el text de dins la etiqueta
    hd= $(data).find("heading").text();

    //el console log es para coprobar 
    //que lo estamos haciendo bien en consola
    console.log(hd);

    //posem el que hi ha dins la etiqueta al h1
    $("h1").html(hd);

});*/